import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  topHeaderView: {
    position: 'absolute',
    backgroundColor: 'transparent',
    zIndex: 1,
  },
  scrolledHeaderView: {
    position: 'absolute',
    zIndex: 1,
  },
})
